//import React from 'react';

// eslint-disable-next-line react/prop-types
function Button({ title }) {
    return (
      <div>
        <button className="btn btn-active btn-neutral">{title}</button>
      </div>
    );
}
  
export default Button;
 
